# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Dependencies
node_modules/
.pnp
.pnp.js

# Git worktrees
.worktrees/

# Build output
dist/
dist-ssr/
.vite/
build/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
.project

# Claude Code local state
.claude/
.classpath
.settings/
*.sublime-workspace
*.sublime-project

# Tests
coverage/
.nyc_output/
*.lcov

# Environment variables (NEVER COMMIT)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Temporary files
*.tmp
*.temp
.cache/
.parcel-cache/

# Supabase
.branches/
.temp/

# Vercel
.vercel

# Lockfiles (using npm — ignore others)
bun.lockb
yarn.lock
pnpm-lock.yaml

# Deno lock file (generated by `deno check` / `deno test`)
deno.lock

# Misc
*.bak
*.orig

# Onboarding tutorial video — too large for GitHub (100 MB limit).
# Host on a CDN (Vercel Blob / Bunny / Cloudinary) before prod deploy
# and replace the import in TutorialVideoModal with a URL string.
src/assets/onboarding/tutorial.mp4
